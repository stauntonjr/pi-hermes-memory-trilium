// Example usage of pi-hermes-memory-trilium
import { ExtensionAPI } from "@earendil-works/pi-coding-agent";
export function setupTriliumSync(pi) {
    // Extension is automatically loaded by Pi
    // No manual setup needed if installed via pi install
    // To manually trigger sync, use the command:
    // /trilium-sync
    console.log("Trilium sync extension loaded");
}
//# sourceMappingURL=example-usage.js.map